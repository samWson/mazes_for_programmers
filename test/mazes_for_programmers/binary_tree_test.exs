defmodule MazesForProgrammers.BinaryTreeTest do
  use ExUnit.Case, async: true

  test "says hello" do
    assert MazesForProgrammers.BinaryTree.hello() == "Hello. This is where the maze goes."
  end
end
